# database init, supports mysql too
database=hsqldb
# MIGRATED: Replaced deprecated spring.datasource.schema with spring.sql.init.schema-locations (Spring Boot 2.5+)
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
# MIGRATED: Replaced deprecated spring.datasource.data with spring.sql.init.data-locations (Spring Boot 2.5+)
spring.sql.init.data-locations=classpath*:db/${database}/data.sql

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none

# Internationalization
spring.messages.basename=messages/messages

# Actuator / Management
management.endpoints.web.base-path=/manage
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
# MIGRATED: Replaced deprecated spring.resources.cache with spring.web.resources.cache (Spring Boot 2.6+)
spring.web.resources.cache.cachecontrol.max-age=12h
