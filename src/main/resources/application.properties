# database init, supports mysql too
database=hsqldb
# MIGRATED: Updated from deprecated spring.datasource.schema/data to spring.sql.init.* (Spring Boot 2.5+)
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
spring.sql.init.data-locations=classpath*:db/${database}/data.sql

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none
# MIGRATED: Added spring.jpa.defer-datasource-initialization=true to ensure SQL scripts run after JPA init (Spring Boot 2.5+)
spring.jpa.defer-datasource-initialization=true

# Internationalization
spring.messages.basename=messages/messages

# Actuator / Management
management.endpoints.web.base-path=/manage
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
# MIGRATED: Updated from deprecated spring.resources to spring.web.resources (Spring Boot 2.6+)
spring.web.resources.cache.cachecontrol.max-age=12h
