# database init, supports mysql too
database=hsqldb
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
spring.sql.init.data-locations=classpath*:db/${database}/data.sql
# MIGRATED: replaced deprecated spring.datasource.schema/data with spring.sql.init.* (Spring Boot 2.5+ property names)

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none

# Internationalization
spring.messages.basename=messages/messages

# Actuator / Management
management.endpoints.web.base-path=/manage
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
spring.web.resources.cache.period=12h
# MIGRATED: replaced deprecated spring.resources.cache.cachecontrol.max-age with spring.web.resources.cache.period (Spring Boot 2.6+ property name)
